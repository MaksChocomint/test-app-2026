# 🐾 HappyPets — Ветеринарная Клиника

<img src="/public/cat/cat_idle.webp" width="200" alt="HappyPets Cat" />

Добро пожаловать в **HappyPets** — современное приложение для управления ветеринарной клиникой. Данный проект сфокусирован на реализации удобной системы **медицинских карточек** питомцев, позволяя врачам эффективно отслеживать историю болезни .

---

## 🎨 Айдентика и стиль

Приложение выполнено в уникальной цветовой палитре, идеально подходящей вет-клинике и подчеркивающей дружелюбие и профессионализм:

- 🟢 **Mint:** `#4cbab3` (Основной бренд)
- 🟠 **Orange:** `#f49e3f` (Акценты)
- ⚪ **Beige:** `#fdfcfb` (Фон)
- 🌑 **Dark:** `#1a3d42` (Текст)
- 💧 **Mint Light:** `#d2f3f0` (Вторичные элементы)

---

## 🛠 Технологический стек

| Слой                   | Технологии                              |
| ---------------------- | --------------------------------------- |
| **Framework**          | **Next.js v16** (App Router), React v19 |
| **State Management**   | **Zustand v5**                          |
| **Database & ORM**     | **PostgreSQL**, **Prisma v7**           |
| **Styling**            | **Tailwind CSS v4**, Lucide-react       |
| **Forms & Validation** | **React Hook Form**, **Zod**            |
| **API Client**         | **Axios**                               |
| **Infrastucture**      | **Docker**, **Bun**                     |

---

## 🚀 Инструкция по запуску

Для работы приложения необходимы: **Node.js (v22+)**, **Docker** и желательно **Bun**.

### 1. Клонирование репозитория

```
git clone https://github.com/MaksChocomint/test-app-2026.git
cd test-app-2026
```

### 2. Запуск базы данных

Используется PostgreSQL внутри Docker-контейнера.

```
docker-compose up db -d
```

### 3. Настройка окружения и БД

Установите зависимости и синхронизируйте схему Prisma:

```
# Установка зависимостей
bun install # или npm install

# Генерация клиента Prisma
bunx prisma generate # или npx prisma generate

# Применение миграций (введите название, например "init")
bunx prisma migrate dev # или npx prisma migrate dev

# Наполнение базы тестовыми данными (ОБЯЗАТЕЛЬНО)
# Так как реализован CRUD именно карточек, нужны предзаполненные владельцы, питомцы, ветеринары и записи на осмотр
bunx tsx prisma/seed.ts # или npx tsx prisma/seed.ts
```

### 4. Запуск приложения без Docker

После того как база данных запущена и **сидирование (seed) завершено**, можно запустить само приложение:

```
# Запуск без Docker
bun run dev # или npm run dev
```

### 5. Запуск приложения через Docker

После того как база данных запущена и **сидирование (seed) завершено**, можно запустить само приложение:

```
docker-compose up app --build -d
```

_Примечание: Dockerfile приложения не включает в себя сидирование, это действие выполняется клиентом вручную на этапе настройки._

---

## 📊 Модель данных

В основе приложения лежит продуманная реляционная структура, описывающая взаимоотношения между владельцами, их питомцами, записями на прием и медицинскими записями, составляющими **медицинскую карточку**.

_Схема ER-Диаграммы_
![[er-diagram.png]]

---

## 📂 Структура проекта

- `app/` — Основная логика Next.js (маршруты, layout).
  - `api/` — **Server Actions & Endpoints**: GET, POST, DELETE, PUT запросы.
  - `generated/` — Артефакты Prisma Client.
- `components/` — Библиотека переиспользуемых React-компонентов.
- `store/` — Глобальное состояние приложения на **Zustand**.
- `prisma/` — Схема БД, миграции и скрипт `seed.ts`.
- `lib/` — Утилиты, валидация и конфигурация API.
- `hooks/` — Кастомные хуки (в т.ч. `useDebounce` для оптимизации поиска в случае частых запросов на сервер при фильтрации).
- `types/` — Описание TypeScript интерфейсов и объектной модели (objectModel.ts).
- `public/` — Статические ресурсы (логотипы, изображения).

---

## 💻 Функционал

✅ **Управление питомцами**: Поиск и выбор пациента.

✅ **Медицинская карта**: Просмотр полной истории посещений в хронологическом порядке.

✅ **Полный CRUD**: Создание новых записей осмотра в медицинскую карточку, редактирование существующих и удаление ошибочных данных.

✅ **Валидация**: Строгая проверка вводимых данных на клиенте с помощью Zod.

---

**Разработано с ❤️** Если у вас возникли вопросы по запуску, свяжитесь со мной!

TELEGRAM: t.me/makschocomint
EMAIL: makschocomint@gmail.com
